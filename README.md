# To-Do-List-Application
This project is a simple command line To Do List application written in Python. It allows users to manage daily tasks by adding, viewing, completing, and removing tasks. The program also saves tasks in a file so the list remains available when the program runs again
